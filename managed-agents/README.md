# Managed Agent definitions

Reviewed, repo-side **source** for the primary Djonik Agent and its separate Project Health specialist. The live Claude Managed Agent resource stays authoritative; a file here is not applied automatically, is not read by any runtime code, and does not create a second Djonik identity.

Each file uses the official declarative `ant apply` format: YAML frontmatter is the agent configuration, the body is the `system` prompt.

## Audit / release qualification — 2026-09-21

The v21/v4 statements below describe recorded remote resource state from the rollback report, not fresh proof of the active Telegram Session. #33 will pin accepted coordinator Skill versions and attest application revision, resolved serving Session and specialist pins. No definition, lock or remote resource is changed by the governance update.

#29's future capability-ownership experiment is isolated; an extension of the existing PH role is only a candidate. Keep the accepted definition unchanged until candidate acceptance and an authorized rollout. New reliability evidence in [docs/17](../docs/17_DJONIK_V1_ARCHITECTURE_AUDIT.md) requires #31/#32/#33 follow-up even though historical #28 acceptance remains valid for its tested scope.

## Conventions

- One file per agent. Keep it minimal, and do not duplicate Skill contents in the system prompt.
- Pin Skills and referenced agents to explicit versions. Nothing is inherited from the coordinator, so declare every capability the agent needs.
- Enable tools by allowlist: `default_config: {enabled: false}`, then list what is needed.
- Applying a file (`ant apply` or `agents.create`) is a live change and needs explicit Product Owner authorization.
- Apply one named file from the repository root (`ant apply managed-agents/<file>.md`), never `ant apply .`. The file sits outside the conventional `agents/` directory, so `ant apply` only treats it as an agent because it is named explicitly. `claude-lock.json` at the repository root records which live resource each file created; do not edit it by hand.

## Files

- `project-health-specialist.md`: read-only Sonnet Project Health specialist (issue #28, Wave C1). **Exists as a live standalone agent** (`agent_01KNiQDzzPjaMU6LLF4mU6uM`); the live version is **v4** (final-answer discipline hardening after the §44 production smoke — user-facing-only final message, no coordinator/technical note, no pool counts or unsupported actor, one next step; system body only, frontmatter/config identical to v3; recorded in `claude-lock.json`, `docs/15_ISSUE_28_IMPLEMENTATION_REPORT.md` §45). v3 was the natural-PM response-shape hardening. **Specialist v3 passed delegated live validation** ("Scenario A", `docs/15_ISSUE_28_IMPLEMENTATION_REPORT.md` §42): native delegation, factual correctness, natural-PM voice/shape, and exactly-one-final-child-result all passed against fresh Trello evidence. **Production Djonik (`agent_01WGRHDBjQa3eMhoGJMmQ1dh`) was v19 at Wave C1 acceptance and later v21 after the #29 rollback** (v20 attached the coordinator-only `work-review` Skill for #29; that attach failed live acceptance and was rolled back, so v21 is semantically identical to v19 and `work-review` is not part of production; the Project Health specialist stays v4 and the #28 acceptance is unchanged) and pins specialist v4 through its own native roster (v18 → v19 changed only that roster pin, §45; the v18 cutover is §43); the production coordinator no longer owns the `project-health` Skill directly — the specialist exclusively owns it. **Specialist v4 and production v19 are accepted**: the final live acceptance (`docs/15_ISSUE_28_IMPLEMENTATION_REPORT.md` §48) passed through the real `connectToDjonik()` path, including the deterministic reply-boundary safeguard in `src/djonikClient.ts` (§47). The temporary, non-production validation coordinator (`agent_01GFCLFYq6uLRHG8vMCrAgeK`, v5, pinned to historical specialist v3) was **archived** at closeout (§49) and is retained only as historical validation infrastructure; it is not part of production, was never repinned to v4, and no v6 exists. No further production smoke is pending for this agent. A coordinator roster pins a specialist version, so any later specialist change also needs a separate production roster update.

- `djonik.md`: reviewed source for the **primary Djonik coordinator** (`agent_01WGRHDBjQa3eMhoGJMmQ1dh`). **Current state (2026-09-23): production v25**, Claude Sonnet 5 / effort medium / speed standard (frozen #38 baseline). v24 → v25 changed only `system` to the accepted source prompt. Fresh read-back confirmed it byte-identical (3529 B; SHA-256 `3e204453c82c87aa82404f5007f872a93bd2778a22ec97a0b6c0fd1224b5a4b6`). All other fields were unchanged from v24, including the specialist v4 roster. The three coordinator Skills `task-management`, `daily-planning` and `weekly-planning` now carry new versions that are byte-identical to the repo source. The new task-management version also brings the accepted #37 label rules to production for the first time. `studio-intake` already matched and is unchanged. Details: [docs/42](../docs/42_ISSUE_38_REMOTE_SYNC_REPORT.md).
  - An isolated zero-inference Session (`sesn_015pyFLex524B9ASrk8fxHHP`, pinned v25, Memory `read_only`) is prepared for the contract-derived smoke under [docs/41](../docs/41_ISSUE_38_CONTRACT_DERIVED_RUBRIC.md). That smoke has not run. #38 remains canonical NOW and unaccepted.
  - History: the v21 → v22 model migration, v22 → v23 prompt sync, v23 → v24 effort change (prompt `fa361ed9…817f`) and failed low/medium smokes are in [docs/39](../docs/39_ISSUE_38_SONNET_5_LOW_MODEL_MIGRATION_REPORT.md). The live Agent remains authoritative. `ant apply` on this untracked source file would create a second Agent.
  - The Telegram serving Session has **not** been promoted to v25 or attested; #33 owns that transition. New model/system settings take effect in newly created Sessions. Sessions record these Skills as `latest`, and it is unverified whether an existing Session re-resolves them.
  - `model` uses object form to state `id`, `effort` and `speed` explicitly;
  - production's `agent_toolset_20260401` is **default-enabled with no allowlist** (unlike the specialist and unlike the convention above). That is reproduced as-is, not silently tightened. It is why the docs/34 smoke could reach built-in `bash`; whether the coordinator needs `bash` at all is an open configuration question for the Product Lead, deliberately outside #38 scope;
  - `claude-lock.json` has no entry for this file because the production migration was a direct `agents.update`, not `ant apply`. The current source prompt was applied by direct `agents.update` in v25.
  - Source invariants are pinned by `src/djonikAgentSource.test.ts`; they test concepts, not exact prose.
