{
  "name": "djonik-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Djonik 2.0 — thin Node/TypeScript client for the existing Djonik Claude Managed Agent (Foundation 2).",
  "type": "module",
  "scripts": {
    "dev": "tsx src/cli.ts",
    "telegram": "tsx src/telegramCli.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/telegramCli.js",
    "release:check": "tsx src/releaseCheck.ts",
    "telemetry:reconcile": "tsx src/reconcileTelemetry.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "dotenv": "^17.2.3",
    "grammy": "^1.46.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
